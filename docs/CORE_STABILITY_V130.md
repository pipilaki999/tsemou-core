# TSEMOU Core v1.3.0 Stable

Fixes:
- Company directory shortcode now registers a real callable method.
- [tsemou_company_directory] forces the new UX renderer.
- Adds init-level shortcode registration fallback.
- Keeps only one needed frontend shortcode for Companies page.

Use:
[tsemou_company_directory]
