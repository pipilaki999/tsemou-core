# v2.3.6 Inline Admin TSEMPORT Preview

Fix:
Do not depend on preview buttons or frontend URLs.

Behavior:
When Developer Console diagnostics run with a Company ID, the Clean TSEMPORT renderer appears inline inside wp-admin.

Use:
Developer Console -> Company ID 7723 -> Run Diagnostics.
